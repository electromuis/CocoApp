mutation RespondInvite($room: ID!, $accepted: Boolean!) {
    inviteResponse(
        roomId: $room,
        accepted: $accepted
    ) {
        accepted
        room {
            id
        }
    }
}